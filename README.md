# command_console
Minimal Flask console to run Python/PowerShell scripts with one‑click buttons. All config from .env (SCRIPTS_JSON, GROUPS, ACTIVE_TAB, DEBUG). Tabbed dark UI; shows command output; add scripts by editing .env.
