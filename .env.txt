
# Command Console (.env)
# Keep everything in this file; no extra JSON files needed.

DEBUG=true
ACTIVE_TAB=income
GROUPS=income,general

# Single-line JSON, wrapped in single quotes so python-dotenv parses it cleanly.
# Use forward slashes to avoid escaping backslashes.
SCRIPTS_JSON='[{"label":"insert_label","path":"insert scriptfilepath","type":"language(python)","group":"whattabitsin"}]'
